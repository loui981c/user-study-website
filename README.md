# user-study-website - baseline
Only make changes to main branch, then commit the changes and locally run npm run deplopy to push the changes to the deploy on the website name configured in the project
